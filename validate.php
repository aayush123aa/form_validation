<?php

$fname= $_POST["aayush_fname"];
$lname= $_POST["aayush_lname"];
$email= $_POST["aayush_email"];
$age= $_POST["aayush_age"];
$gender= $_POST["aayush_gen"];
$faculty=$_POST["aayush_faculty"];
$num=$_POST["aayush_con_num"];

$fname_pattern="//"
$lname_pattern=
$email_pattern=
$age_pattern=
$gender_pattern=
$faculty_pattern=
$number_pattern=

if(empty($fname) || empty($lname) || empty($age) || empty($gender) || empty($faculty) || empty($email) || empty($num) || )
{
    echo "Please fill all the fields";
}
else
{
    
}

?>